# Training data placeholder

Place paired samples under `images/` and `masks/`. A mask must use `<image-stem>_mask.png`. Training data is intentionally not versioned until redistribution rights are documented.
